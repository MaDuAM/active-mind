# ActiveMind

## A Full-Stack Project with Depth

ActiveMind is a personal knowledge and action management system – built to tame the growing flood of information, strategies, and insights generated by daily AI use. It is also a demonstration of my ability to design, architect, and deliver a production-ready full-stack application.

**Live Repository:** [github.com/MaDuAM/active-mind](https://github.com/MaDuAM/active-mind)

---

## What It Does

ActiveMind organizes information into three distinct entry types:

| Type | Purpose |
| :--- | :--- |
| **Knowledge** | Static information, reference material, and insights |
| **Passive** | Trackable actions with status (WAITING, ACTIVE, PAUSED) |
| **Active** | Step-by-step processes with progress tracking |

**Additional features:**
- Full-text search with suggestions and result layering
- Status change history and tracking logs
- Soft delete with trash restore
- Full data loading (pagination tested, removed due to UX trade-offs – planned for Post-MVP)
- Dark mode (system-aware)
- Responsive design (desktop, tablet, mobile)

---

## Tech Stack

| Layer       | Technologies                                      |
|-------------|---------------------------------------------------|
| Frontend    | React, TypeScript, Tailwind CSS, Vite             |
| Backend     | Node.js, Express, TypeScript                      |
| Database    | PostgreSQL, Prisma ORM                            |
| Auth        | express-session, bcrypt, connect-pg-simple        |
| Testing     | Vitest, React Testing Library, MSW, Playwright    |
| Deployment  | Docker-ready, environment-based configuration     |

---

## How I Built It – And What I Learned

I built this project in close collaboration with AI (ChatGPT). But this was not a simple "copy-paste" process. It was a journey of trial, error, and systematic improvement.

**The First Attempt (Failed):** I started with enthusiasm, but without a clear plan. The AI praised every idea, pushed me to code immediately, and optimized parts of the app to enterprise level while other parts remained incomplete. When I tried to fix it, I realized: I had lost control. The app broke, and without Git, there was no way back.

**The Turning Point:** I stopped. I analyzed what went wrong and developed a new system:
- **Plan first, code second:** I refused to write a single line of code until the functional specification was complete.
- **Precise changes, not full files:** Instead of regenerating entire files, I forced the AI to provide exact line changes with context.
- **Options, not answers:** I stopped asking "What is the solution?" and started asking "What are the options, and what are their trade-offs?"
- **Critical questioning:** Every AI suggestion was challenged. If I couldn't explain why a solution worked, I rejected it.

**The Result:** ActiveMind was built in 4 weeks – with full control, clear architecture, and no overengineering. The AI became my partner, not my substitute.

**The Key Insight:** AI is an excellent assistant, but a terrible leader. It praises too quickly, pushes too fast, and optimizes too early. The human's role is to define the vision, make the decisions, and critically question every AI suggestion.

---

## Architecture Reflection

ActiveMind was built as a **modular monolith** – a single deployment with clear internal module boundaries (topics, entries, tracking, auth).

**Why this architecture?**
- Team size: 2 (me + AI) → Microservices would have been overkill.
- Data consistency: Private user data with strong consistency needs → Monolith with ACID transactions was the right choice.
- Evolvability: The domain was still evolving → Modular boundaries allowed later extraction if needed.
- Operational simplicity: One deployment, one database, one log – without distributed system overhead.

**Trade-offs I accepted consciously:**
- **No horizontal scaling per module** – the whole app scales together, not individual features.
- **All data loaded at once** – after testing pagination, it performed worse and broke the fluid UX. I chose the solution that made the app feel right in daily use, knowing it will need revision as data grows.
- **Minimal automated testing** – I decided to test manually during development instead of investing in a full automated test suite, given the 4-week timeframe and single-user scope.

**What I learned:**
- Architecture is not a free choice – it is a consequence of requirements and constraints.
- Some decisions cannot be planned on paper – they only become visible when the system is used.
- A working, fluid product beats a theoretically perfect architecture that nobody enjoys using.

**What I would do differently next time:**
- Define a clear API contract (OpenAPI) before implementation.
- Plan for data growth from day one – but validate the solution against real user experience, not just theory.
- Set up automated tests earlier – even if minimal, they catch regressions I cannot see manually.
- Define UI state management before coding – not during.

---

## Testing & Quality Assurance

The project includes a growing test suite to ensure reliability and catch regressions early.

| Test Type | Tools | Status |
| :--- | :--- | :--- |
| Unit / Integration | Vitest + React Testing Library + MSW | 12 tests passing |
| Backend | Vitest + Supertest | Basic auth tests |
| End-to-End | Playwright | Experimental (WIP) |

**Note:** Automated tests are minimal by design – manual testing was prioritized for speed and UX validation during the 4-week build phase.

**Run frontend tests:**
cd packages/frontend && npm test

**Run E2E tests:**
npm run test:e2e

---

## Development & Testing Data

The project includes a seeding script to populate the database with realistic test data:

cd backend
npm run db:seed
# or
npx prisma db seed

What the seed creates:
- 1 admin user (admin / admin123)
- 30 topics with realistic names (Backend, Frontend, DevOps, etc.)
- ~250 entries distributed across all categories
- ~120 entries in the first topic for performance testing
- Realistic statuses, steps, benefits, and pause reasons

---

## Code Quality & Architecture

Backend Structure:
- entries.ts split into route-based modules (routes/entries/)
- Each HTTP method in its own file (get.ts, post.ts, put.ts, delete.ts, status.ts, step.ts, tracking.ts)
- Clean separation of concerns – easier to test and maintain

Frontend Optimizations:
- LoadingOverlay component with backdrop blur for consistent UX
- useLoadingDebounce hook to prevent overlay flicker on fast loads
- useSectionState hook with auto-expand only once (prevents overriding manual user toggles)
- queryClient.clear() on auth actions to prevent cross-user data leaks

What I Would Do Differently Next Time:
- Start with a clearer API contract (OpenAPI/Swagger) before implementing
- Add end-to-end tests earlier in the process
- Use feature flags for faster iteration on experimental UI changes
- Set up CI/CD from day one

## About the Author

I am a graduate in Applied Computer Science (B.Sc.) with a background in design and operations. I build systems – technical, personal, or strategic – with a focus on clarity, maintainability, and pragmatic decision-making.

I learned that mastering AI-assisted development is not about prompting better – it's about knowing when to trust, when to question, and when to say "no" to an AI suggestion.

This project reflects that mindset: it is a tool, a statement, and a learning journey all at once.

Built with curiosity, precision, and a critical approach to AI collaboration.
